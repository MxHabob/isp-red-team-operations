# Tests — Python Test Suite

## Responsibility
Unit tests, integration tests, and validation tests for the platform runtime.

## Rules
- Tests must use synthetic data and fixtures — never real credentials or customer data.
- Test the safety, scope, and kill switch enforcement paths.
- Test failure cases, not just success paths.
- Use deterministic seeds for reproducibility.
